let couleurs = ["rouge", "vert", "bleu", "jaune", "orange"];

/*for (let i = 0; i < couleurs.length; i++) {
  console.log(couleurs[i]);
}*/ // lire le tableau couleurs depuis le début jusqu'à la fin

for (let i = couleurs.length - 1; i >= 0; i--) {
  console.log(couleurs[i]);
} // lire le tableau couleurs depuis la fin jusqu'au début
